/**
 * @aryazos/node-tree
 *
 * A generic, domain-agnostic tree component for displaying hierarchical data.
 * Supports flat (parentId) or nested (children) input formats.
 */

export { NodeTree } from './NodeTree';
export type { NodeTreeProps } from './NodeTree';
export * from './types';
