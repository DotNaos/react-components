# react-components

Extracted view/component collection copied from `aryazos/shared/views/src`.

Current contents:

- view-level packages like calendar, canvas, code editor, markdown editor, pdf viewer, node tree and workspace picker
- shared root exports in `index.ts` and `view.ts`
- separation notes for pure and impure blocks in `README.pure.md` and `README.impure.md`

Current status:

- the source code is copied over
- several packages still point at the old `@aryazos/*` workspace packages
- the next cleanup step is to replace those old package links with the new extracted repos
